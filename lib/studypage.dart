import "package:flutter/material.dart";


class StudyPage extends StatelessWidget {
  const StudyPage() : super();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('お知らせ')),
    );
  }
}